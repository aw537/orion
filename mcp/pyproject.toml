[project]
name = "orion-mcp"
version = "0.1.0"
description = "MCP server for Orion — persistent memory for AI agents"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
orion-mcp = "orion_mcp.server:main"
